## Objetivo
- (1 frase: qual problema resolve)

## O que foi feito
- (bullets curtos)
- (se mudou protocolo/config, cite aqui)

## Como testar
1. `dotnet build -c Release`
2. `dotnet test -c Release`
3. (passos extras, se houver)

## Checklist (DoD v1)
- [ ] Build Release local: `dotnet build -c Release`
- [ ] Testes locais: `dotnet test -c Release`
- [ ] CI verde (GitHub Actions)
- [ ] Sem arquivos de build versionados (`bin/`, `obj/`)
- [ ] Sem segredos/config local commitados (ex.: `Config/runtime.json`)
- [ ] Código segue padrões do repo (.editorconfig)
- [ ] Mudança é pequena e revisável (sem “mega commit”)
- [ ] Atualizei documentação/config se necessário

## Notas / Dívida técnica
-
